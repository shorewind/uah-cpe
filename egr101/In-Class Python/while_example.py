# while example

# query the user on how many times to run the calculator

userInput = input('Do you want to run the program again? (y/n) ')

while userInput != 'no':
    # deep space calculator
    userInput = input('Do you want to run the program again? (y/n) ')
